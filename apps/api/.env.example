# Database Configuration - AWS RDS PostgreSQL (Shared for Local & Production)
# Both local development and production use the SAME cloud database

# Connection String
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@thesimplefund.cbi2qo8w6bsm.us-east-2.rds.amazonaws.com:5432/postgres"

# Direct Connection (same as DATABASE_URL for AWS RDS)
DIRECT_URL="postgresql://postgres:YOUR_PASSWORD@thesimplefund.cbi2qo8w6bsm.us-east-2.rds.amazonaws.com:5432/postgres"

# JWT Secret - Generate a secure random string
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-secure-random-string-here-change-this"

# Stellar Network Configuration
STELLAR_NETWORK="testnet"

# API Configuration (optional)
PORT=3001
